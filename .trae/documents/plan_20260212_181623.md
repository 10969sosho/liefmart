I will proceed with the following plan to implement the requested changes and fixes:

1. **Database Reset & Import (Priority)**

   * **Wipe Database:** Run `php artisan db:wipe` to clear the current database.

   * **Import SQL:** Import the provided SQL dump `/Users/10969sosho/PROJECT/CVSS/Pak Rudi/Liefmart/UPDATE 12.2.26 JAM 17.58.sql` into the database (assuming database name `liefmart` or `siap` - I will verify connection first).

2. **Shop Renaming & Removal**

   * **Rename:** Update "Tiktok Trubleu" to "Tiktok Liefmarket" and "Shopee Trubleu" to "Shopee Liefmarket" in all occurrences (database strings via SQL update if needed, and code references in `manual.blade.php`, `choose.blade.php`).

   * **Remove:** Delete all code and configurations for **Tokopedia**, **Blibli**, and **Lazada Lamour**. This includes:

     * Controllers: `PembayaranTokopediaController`, `PembayaranBlibliController`, `LazadaFinanceController`, etc.

     * Models: `TokopediaFinancialTransaction`, `BlibliFinancialTransaction`, `LazadaFinancialTransaction`, etc.

     * Views: `resources/views/financial/tokopedia`, `blibli`, `lazada`, etc.

     * Routes: Remove related routes from `routes/web.php`.

3. **Analytics Export Fix (Tiktok Order Number)**

   * **Analyze:** Check `SalesAnalyticsController.php` for the export logic.

   * **Fix:** Force Tiktok order numbers to be treated as strings in the Excel export (e.g., by prepending `'` or using explicit string formatting) to prevent scientific notation issues.

4. **Finance Partial Return Logic**

   * **Refine Logic:** Update `ReturFinanceService.php` to handle partial returns more accurately.

   * **Implementation:** Ensure `handlePartialRefundWithDeduction` correctly calculates `adjustedNominalFix` and updates `outstanding` balance, reflecting the refund amount properly.

5. **PKP/Non-PKP Calculation Fix**

   * **Analysis:** Review the order `#582067093053801751` (from the SQL dump) and current tax logic.

   * **Implementation:** Update `ReturFinanceService.php` (and potentially `FinanceAnalyticsController.php`) to ensure **Online Orders** also respect the PKP/Non-PKP logic based on `tax_id` (1.11 divisor for PKP).

   * **Verification:** Create a test case or report to verify the calculation for the specific order.

